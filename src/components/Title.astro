---
interface Props {
  unfocused?: boolean;
}

const { unfocused } = Astro.props;

const Element = unfocused ? "p" : "h1";
---

<Element class="font-serif text-3xl text-n-100 sm:text-5xl">
  <slot />
</Element>
