---
interface Props {
  unfocused?: boolean;
}

const { unfocused } = Astro.props;

const Element = unfocused ? "p" : "h1";
---

<Element class="text-n-100 font-serif text-3xl font-medium sm:text-5xl">
  <slot />
</Element>
