---
import { cn } from "~/lib/style";

interface Props {
  href: string;
  class?: string;
}

const { href, class: className } = Astro.props;

const isExternalLink = href.startsWith("https://");
---

<a
  href={href}
  {...isExternalLink ? { target: "_blank" } : {}}
  class:list={cn(
    "text-n-90 underline decoration-r-50 decoration-wavy hover:opacity-85",
    className
  )}><slot /></a
>
