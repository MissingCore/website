---
import { Text } from "~/constants/Style";
import { cn } from "~/lib/style";

interface Props {
  id?: string;
  class?: string;
}

const { id, class: className } = Astro.props;
---

<h3 id={id} class:list={cn(Text.h3, className)}><slot /></h3>
