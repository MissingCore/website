---
import { cn } from "~/lib/style";

interface Props {
  id?: string;
  class?: string;
}

const { id, class: className } = Astro.props;
---

<h1 id={id} class:list={cn("font-serif text-5xl text-n-100", className)}>
  <slot />
</h1>
