---
import { cn } from "~/lib/style";

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<blockquote
  class:list={cn(
    "border-l-r-50 text-n-95 border-l-2 pl-2 text-sm font-semibold",
    className
  )}
>
  <slot />
</blockquote>
