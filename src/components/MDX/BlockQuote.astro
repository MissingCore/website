---
import { cn } from "~/lib/style";

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<blockquote
  class:list={cn(
    "border-l-2 border-l-primary pl-2 text-sm font-semibold text-onSurfaceVariant",
    className,
  )}
>
  <slot />
</blockquote>
