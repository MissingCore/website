---
import { cn } from "~/lib/style";

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<blockquote
  class:list={cn(
    "border-l-2 border-l-r-50 pl-2 text-sm font-semibold text-n-95",
    className
  )}
>
  <slot />
</blockquote>
