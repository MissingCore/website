---
import { cn } from "~/lib/style";

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<ul
  class:list={cn(
    "ml-4 flex list-disc flex-col gap-2 pl-4 text-n-80",
    className
  )}
>
  <slot />
</ul>
