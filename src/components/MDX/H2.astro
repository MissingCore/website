---
import { cn } from "~/lib/style";

interface Props {
  id?: string;
  class?: string;
}

const { id, class: className } = Astro.props;
---

<h2 id={id} class:list={cn("text-n-100 font-serif text-3xl", className)}>
  <slot />
</h2>
